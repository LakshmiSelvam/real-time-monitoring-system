let version = "1.0.0-0.1.0";

// let domain = window.location.origin + "/api";
let domain = "https://demo-apps.sitesenz.com/auth/services/api/v1"
// if (!process.env.NODE_ENV || process.env.NODE_ENV === "development") {
//   domain = "https://demo-apps.sitesenz.com/auth/services/api/v1";
// }
export default {
  domain: domain,
  version: version,
};
